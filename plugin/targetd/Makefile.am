if WITH_TARGETD

plugindir = $(pythondir)/lsm/plugin
targetddir = $(plugindir)/targetd

targetd_PYTHON = \
	__init__.py \
	targetd.py

dist_bin_SCRIPTS = targetd_lsmplugin
EXTRA_DIST = targetd_lsmplugin.in
endif
