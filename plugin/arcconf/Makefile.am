if WITH_ARCCONF
plugindir = $(pythondir)/lsm/plugin
arcconfdir = $(plugindir)/arcconf

arcconf_PYTHON = __init__.py arcconf.py utils.py

dist_bin_SCRIPTS = arcconf_lsmplugin
EXTRA_DIST = arcconf_lsmplugin.in
endif
